# vim: filetype=tmux
# Vi-style keybinds
set -g status-keys vi
set -g mode-keys   vi

set -g prefix C-a

set  -g base-index 1
set  -g bell-action none
set  -g display-time 1500
set  -g display-panes-time 800
set  -g history-limit 10000
set  -g mouse on
set  -g renumber-windows on

# Allow terminal passthrough sequences when needed by apps.
set-option -g allow-passthrough on

set-option -g detach-on-destroy off

set-option -g automatic-rename on
set-option -g automatic-rename-format '#{window_icon}'

set -sg repeat-time 600

set -g visual-activity off
set -g visual-bell off
set -g visual-silence off

setw -g aggressive-resize off
setw -g pane-base-index 1

setw -g monitor-activity off
setw -g monitor-bell off

set  -s escape-time 0
set  -s focus-events on

set -g main-pane-width 90

# Status line (two lines for vertical breathing room)
set -g status 2
# Keep default first line; use a blank second line for vertical padding.
set -g status-format[1] ""
set -g status-interval 5
set -g status-position top
set -g status-justify left
set -g status-left-length 100

set -g window-status-format ' #I #W '
set -g window-status-current-format '❬ #I #W ❭'
set -g status-left ' 🚀 #S ┃ '
set -g status-right ''

set-window-option -g window-status-separator ' ┃ '

set -g set-titles on
